@model Movie
@{
}

<partial name="_MoviePartialView.cshtml" />


@*{

    string platStr = "Platform";
    if (Model.Platform.Count > 1) platStr += "s";
    platStr += ": ";
    for (int i = 0; i< Model.Platform.Count; i++)
    {
        platStr += Model.Platform[i];
        @if(i<10)
                {

                }
    }

}*@


<!--
if (Model == null)
{
<h1>No movie provided</h1>
}
else
{
<div>
    <h3>@*Model.Title*@</h3>
    <h4>Year: @*Model.Year*@</h4>
    <h5>Rating: @*Model.Rating*@</h5>
</div>
}
    -->
